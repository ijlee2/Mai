$(document).ready(function() {
    // Navbar for mobile
    $(".button-collapse").sideNav();

    // Carousel settings
    $(".carousel").carousel({
        "duration": 120,
        "dist"    : -60,
        "shift"   : 80
    });
});